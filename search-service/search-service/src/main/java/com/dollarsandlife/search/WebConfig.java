package com.dollarsandlife.search;

import org.springframework.context.annotation.Configuration;

/**
 * Web configuration. CORS is handled by CorsHeaderFilter so that
 * we only add headers and never reject with 403.
 */
@Configuration
public class WebConfig {
}
