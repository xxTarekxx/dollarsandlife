# ── Deployment configuration template ────────────────────────────────────────
# Copy this file to deploy.env.ps1 and fill in your actual values.
# deploy.env.ps1 is gitignored — deploy.env.ps1.example is safe to commit.

$VPS_HOST = "root@YOUR_VPS_IP"
$VPS_ROOT = "/var/www/html/YOUR_SITE"
